# AI Provider Keys
gemini.api.key=${GEMINI_API_KEY}
openai.api.key=${OPENAI_API_KEY}
deepseek.api.key=${DEEPSEEK_API_KEY}

# Financial API Keys
fmp.api.key=rAdZOxyWt2Om8MzsZeE3PMQ3mnA8dy13
tiingo.api.key=9e5914bbe6bb8dada138bcc2813b7c2c4a893a42

# SQLite Connection
spring.datasource.url=jdbc:sqlite:./data/market_data.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.initialization-mode=never
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=none

# Resilience: WAL mode & Single Connection (prevents "Database Locked")
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.connection-init-sql=PRAGMA journal_mode=WAL;

# Graceful Shutdown
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=15s

# This allows the app to find your .env file in the project root during normal runs
spring.config.import=optional:file:.env[.properties]
